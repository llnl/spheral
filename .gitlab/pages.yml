create-pages:
  extends: [.toss_page_resource]
  variables:
    GIT_SUBMODULE_STRATEGY: normal
  stage: deploy
  # Prevent this job from collecting artifacts
  dependencies: []
  pages:
    # The folder that contains the files to be exposed at the Page URL
    publish: public
  rules:
    # This ensures that only pushes to the default branch will trigger
    # a pages deploy
    - if: '$CI_PIPELINE_SOURCE == "schedule" && $SCHEDULE_TAG == "deploy"'
      when: always
    - if: '$CI_COMMIT_MESSAGE =~ /test-deploy/'
      when: always
  script:
    - source $HOME/docsvenv/bin/activate
    - srun -n 8 --cpus-per-task 14 python3 $SCRIPT_DIR/lc/plot_hist_data.py --threads 14
  artifacts:
    paths:
      - public
